# Edge VLM Workflow Implementation Matrix

| Requirement ID | Original Intent | Current Status | Implementation Pointer | Verification Pointer | Notes |
|---|---|---|---|---|---|
| R1 | Inspect current repo and preserve existing structure | Implemented | Initial repo had only `.vscode/settings.json`; new structure created | `git log --oneline`; repo status | No prior project code existed. |
| R2 | Inspect OrangePi reference and record lessons | Implemented | `docs/reference_notes/orangepi_minicpmv46_notes.md` | Manual reference inspection at commit `490d039` | Ascend-specific code documented as non-portable. |
| R3 | Use llama.cpp GGUF as first backend | Implemented | `docs/runtime_matrix.md`, `scripts/wsl/*.sh`, `scripts/jetson/*.sh` | `bash -n scripts/...`; local `llama-server --version`; Gemma Q8 CPU text smoke; Gemma Q8/Q4 CUDA text and sample-image benchmark; MiniCPM Q4 CUDA text and sample-image benchmark; copied Jetson Q4 benchmark JSONL; CUDA build verification; tracked Jetson formal/repeat benchmark docs | Gemma Q8/Q4 and MiniCPM-V 4.6 Q4 artifacts are present. WSL smoke passed on CPU fallback and CUDA llama.cpp. CUDA build wrappers are verified with `GGML_CUDA=ON`. Jetson MiniCPM/Gemma Q4 now have formal/repeat evidence with max-clocks comparisons. Jetson Q8, camera input, long-run behavior, and broad runtime portability remain unverified. |
| R4 | Keep WSL and Jetson scripts separate | Implemented | `scripts/wsl/`, `scripts/jetson/`, `scripts/common/` | `bash -n` | Docker path is Jetson-oriented. |
| R5 | Keep runtime configs separate from code | Implemented | `configs/models/*.yaml` | Config parse check in `transformers` env | `.gitignore` fixed to track configs. |
| R6 | Implement OpenAI-compatible client | Implemented | `src/edge_vlm/client.py`, `src/edge_vlm/image_payload.py` | `tests/test_edge_vlm.py` | Uses standard-library HTTP. |
| R7 | Treat image support as backend-sensitive | Implemented | `capabilities.image`, client/benchmark checks | Unit tests; Gemma Q8/Q4 and MiniCPM Q4 WSL CUDA sample-image runs; copied Jetson Q4 sample-image JSONL | Runtime server capability is observed for Gemma Q8, Gemma Q4, and MiniCPM-V 4.6 Q4 on WSL CUDA. Jetson image capability is observed only for MiniCPM-V 4.6 Q4 and Gemma Q4 with the copied smoke logs. |
| R8 | Implement benchmark harness and cases | Implemented | `src/edge_vlm/benchmark.py`, `configs/benchmark/prompt_cases.jsonl`, `configs/benchmark/text_prompt_cases.jsonl`, `data/sample_images/` | Unit tests; dry-run command with `--summary-output` and `--metadata-output` | `fake_stream` case is a marker for the fake-stream runner. `text_prompt_cases.jsonl` is the text/router-only case set. JSONL plus manifest are the raw source of truth; Markdown summaries are readable sidecars. |
| R9 | Implement fake stream prototype | Implemented | `src/edge_vlm/fake_stream.py` | Unit tests; dry-run CLI | Folder input only; no camera. Continues after per-frame errors unless `--stop-on-error` is set. |
| R10 | Create Jetson migration docs and scripts | Implemented | `docs/migration_wsl_to_jetson.md`, `scripts/jetson/*.sh` | `bash -n`; `JETSON_DRY_RUN=1` launcher checks; formal wrapper dry-run; copied and tracked Jetson benchmark docs | Jetson Docker execution is observed for MiniCPM-V 4.6 Q4, Gemma Q4, and selected lightweight GGUF candidates. Dry-run still validates command construction when hardware is unavailable. |
| R11 | Avoid cloning references into source tree | Implemented | `.gitignore` ignores `tmp/`; clones under `tmp/references/` | `git status --ignored` | Reference repos not committed. |
| R12 | Validate without overstating runtime support | In progress | Tests, syntax checks, dry-run paths, sample image payload checks, Jetson dry-run checks, WSL CUDA smoke outputs, Jetson benchmark docs, `docs/specs/next_phase_infra_and_model_strategy.md` | Unit tests, `bash -n`, `py_compile`, `git diff --check`, tracked benchmark reports under `docs/benchmarks/` | Current docs distinguish smoke, formal repeat, candidate, official artifact, third-party artifact, and failed-artifact evidence. The full project goal remains open because Jetson Q8, camera input, long-run behavior, and runtime-lane implementation are not complete. |
| R13 | Formal benchmark infra with manifests and profile pointers | Implemented | `src/edge_vlm/benchmark.py`, `scripts/jetson/run_formal_benchmark.sh`, `docs/benchmark_protocol.md` | Unit tests for run metadata/repeated trials/formal wrapper dry-run; `bash -n` | Benchmark JSONL records include `run_id`, `trial_index`, and `case_index`; manifests record benchmark arguments, environment pointers, profile paths, and Jetson sidecar locations. |
| R14 | Optimization loop with sanity guard and structured profiling | Implemented | `docs/specs/jetson_optimization_loop.md`, `src/edge_vlm/optimization.py`, `src/edge_vlm/jetson_profile.py`, `src/edge_vlm/jetson_sweep.py`, `scripts/jetson/phase_logging.sh`, `scripts/jetson/run_optimization_sweep.sh`, `configs/benchmark/jetson_optimization_variants.jsonl` | Unit tests for quality guards, fake-stream guard, preflight lfb skip, profile summaries, lifecycle phase merge, warmup classification, shutdown timing, input timing summary labels, comparison columns, and variant coverage | The sweep loop ranks only guard-passing rows, links structured `tegrastats` profile summaries, records startup, shutdown, warmup policy, launcher artifact-check/download timing, and benchmark/fake-stream input timing summaries when available, parses `GR3D_FREQ`/`EMC_FREQ`/lfb/power/temp, and reports conservative bottleneck labels. |
| R15 | Fixed comparison policy for Jetson repeatability | Implemented | `scripts/jetson/run_remote_optimization_sweep.sh`, `scripts/jetson/run_remote_current_defaults_suite.sh`, `docs/benchmarks/jetson_isolated_repeats_20260531.md`, `docs/benchmarks/jetson_optimization_sweep_20260530.md` | Unit tests for remote max clocks, cache-drop forwarding, min-lfb forwarding, and current-default suite command construction | Promotion comparisons use locked clocks, cache drop, `--min-lfb-blocks`, repeated trials, and three-frame fake-stream sidecars. The current evidence says broad llama.cpp flag sweeps should stop for the present MiniCPM/Gemma defaults unless a new profiling claim justifies a scoped probe. |
| R16 | Lightweight GGUF model expansion lane | In progress | `docs/specs/next_phase_benchmark_and_models.md`, `docs/specs/next_phase_infra_and_model_strategy.md`, `scripts/jetson/run_hf_gguf_vlm_llama_docker.sh`, `scripts/jetson/run_hf_gguf_llama_docker.sh`, `scripts/jetson/run_remote_lightweight_model_suite.sh`, `scripts/jetson/run_remote_tencent_text_suite.sh`, `configs/models/smolvlm2_256m_q8.yaml`, `configs/models/qwen3_vl_2b_thinking_q4.yaml`, `configs/models/hunyuanocr_q8.yaml`, `configs/models/youtu_vl_4b_q8.yaml`, `configs/models/youtu_vl_4b_q4_thirdparty.yaml`, `configs/models/tencent_hy_mt1p5_*.yaml`, `configs/models/tencent_hy_mt2_*.yaml`, `docs/benchmarks/jetson_lightweight_models_20260531.md`, `docs/benchmarks/jetson_remote_smoke_20260531.md` | Unit tests for VLM and text model candidate configs, generic HF GGUF VLM/text launcher dry-runs including completed `.partial` resume recovery, text-only fake-stream skip, next-phase spec candidate ordering, lightweight remote suite command construction, Tencent text suite command construction, all-variant text-suite documentation consistency, invalid-Q8 full-suite evidence documentation, and repeat evidence documentation | SmolVLM2 256M and Qwen3-VL 2B have guard-passing smoke evidence; HunyuanOCR Q8 now has Jetson load-path evidence after the launcher-resume fix but failed the guard with repetitive punctuation output and is excluded from ranking/formal repeat until quality triage; Tencent Youtu-VL official Q8/BF16-mmproj failed with Jetson CUDA OOM; third-party Youtu Q4 CPU-mmproj is separated from official Tencent evidence. Tencent Hy-MT1.5 and Hy-MT2 1.8B GGUF quantizations are added only for the text/router lane and are not VLM ranking rows; all seven configured Hy-MT rows are in the dedicated remote text-suite default, low-bit failures are treated as runtime-compatibility evidence, Hy-MT2 Q4 has a guard-passing cached text smoke, Hy-MT2 Q4/Q6 have one valid full-suite text smoke from `tencent-text-smoke64-20260531T120054Z`, `tencent-hy-mt2-q6-smoke64-cached-20260531a` is valid cached Q6 evidence, the Q8 full-suite row is invalidated, `tencent-hy-mt2-q8-smoke64-cached-20260531T132724Z` is valid one-trial Q8 cached evidence, and `tencent-text-repeat5-20260531a` is valid 5-trial Q4/Q6/Q8 text evidence at 34.528/26.778/31.395 tok/s with 20/20 success per valid row. Fixed-policy repeat `lightweight-repeat5-20260531T134554Z` completed for MiniCPM/Gemma baselines plus SmolVLM2, Qwen3-VL 2B, and Youtu Q4 third-party: SmolVLM2 is latency_floor, Qwen is balanced_candidate pending output review, and Youtu Q4/Gemma profile as slow or runtime_overhead rows. |
| R17 | Artifact/runtime image reproducibility | In progress | `docker/llama-cpp/Dockerfile`, `scripts/build_llama_cpp_artifacts.sh`, `scripts/build_llama_cpp_image.sh`, `src/edge_vlm/jetson_sweep.py`, `docs/benchmarks/jetson_remote_smoke_20260531.md` | Unit tests for Docker image metadata capture in sweep plans; image smoke docs with tag/id/ref evidence | Runtime rows capture image tag/id/digest and llama.cpp ref when Docker inspect is available. The canonical `d749821db3bd` image is valid for continued work and newer multimodal support, but is not documented as a performance promotion. |
| R18 | Pipeline, routing, and input-path timing | In progress | `docs/specs/next_phase_infra_and_model_strategy.md`, `src/edge_vlm/image_payload.py`, `src/edge_vlm/client.py`, `src/edge_vlm/benchmark.py`, `src/edge_vlm/fake_stream.py`, `src/edge_vlm/jetson_sweep.py` | Unit tests for image payload timing, client dry-run timing, benchmark `input_timing`, profile `input_timing_summary`, fake-stream record compatibility, fake-stream schedule/backpressure timing, explicit late-frame skip behavior, adaptive interval behavior, and sweep fake-stream control passthrough | Input records now capture image bytes, MIME detection, image read, base64/data URL build, payload build, JSON serialization, request body size, HTTP elapsed time, response parse time when available; profile summaries aggregate those records across formal/fake-stream outputs. Fake-stream supports fixed-cadence schedule delay/backpressure, optional `--skip-late-frames` source-frame drops with `skipped_frames_before`, and optional `--adaptive-interval` cadence experiments with `effective_interval_s`. Full routing policy and camera/live input are still not implemented. |
